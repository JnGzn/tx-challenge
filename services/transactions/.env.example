HTTP_PORT=3000
DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@postgres-transactions:5432/transactions?schema=public
POSTGRES_PASSWORD=change-me
KAFKA_BROKERS=kafka:9092
KAFKA_CLIENT_ID=transactions-service
KAFKA_GROUP_ID=transactions-consumer
# Optional: max rows returned by GET /transactions. Default 100
# TRANSACTIONS_LIST_LIMIT=100
